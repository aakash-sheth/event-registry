'use client'

// WhatsApp setup status is a platform operator concern — this component renders nothing for hosts.
// The CampaignList disables the Launch button when WhatsApp is unconfigured.
export default function WhatsAppSetupBanner() {
  return null
}
